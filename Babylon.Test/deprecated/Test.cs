using NUnit.Framework;
using System;
using Babylon.UI;

namespace Babylon.Test
{
	[TestFixture ()]
	public class TranslationsTest
	{
		/*[Test ()]
		public void TestCase ()
		{
			String[] o = { "Я из Москвы", "Он прилетел в Берлин", "Я приехал из Москвы" };
			String[] t = { "Ich bin aus Moskau", "Er ist nach Berlinn gefrogen", "Ich bin aus Moskau gefarhen" };
			Translations translations = new Translations(Lang.RU, Lang.DE);
			Assert.AreEqual(null, translations.getOriginal());
			Assert.AreEqual(null, translations.getTranslation());

			// one element loop
			translations.add(o[0], t[0]);
			Assert.AreEqual(o[0], translations.getOriginal());
			Assert.AreEqual(t[0], translations.getTranslation());
			translations.next();
			Assert.AreEqual(o[0], translations.getOriginal());
			Assert.AreEqual(t[0], translations.getTranslation());

			// two elements loop
			translations.add(o[1], t[1]);
			Assert.AreEqual(o[0], translations.getOriginal());
			Assert.AreEqual(t[0], translations.getTranslation());
			translations.next();
			Assert.AreEqual(o[1], translations.getOriginal());
			Assert.AreEqual(t[1], translations.getTranslation());
			translations.next();
			Assert.AreEqual(o[0], translations.getOriginal());
			Assert.AreEqual(t[0], translations.getTranslation());

			// three elements loop
			translations.add(o[2], t[2]);
			Assert.AreEqual(o[0], translations.getOriginal());
			Assert.AreEqual(t[0], translations.getTranslation());
			translations.next();
			Assert.AreEqual(o[1], translations.getOriginal());
			Assert.AreEqual(t[1], translations.getTranslation());
			translations.next();
			Assert.AreEqual(o[2], translations.getOriginal());
			Assert.AreEqual(t[2], translations.getTranslation());
			translations.next();
			Assert.AreEqual(o[0], translations.getOriginal());
			Assert.AreEqual(t[0], translations.getTranslation());

			TranslatePresenter translatePresenter = new TranslatePresenter ();
		}*/
	}
}

